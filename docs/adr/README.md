# Architecture Decision Records

This directory contains ADRs for significant architectural decisions.
Each file is immutable once accepted — to reverse a decision, write a
new ADR with `supersedes: ADR-NNN`.

Format: `NNN-short-title.md`, zero-padded to 3 digits.

Use `/dc-adr "<title>"` to draft a new one.
